import React from 'react';
import classes from './App.module.scss'

const Component = () => {
	return (
		<div style={{height: '100vh'}} className={classes.appComponent}>
			ulbi
		</div>
	);
};

export default Component;